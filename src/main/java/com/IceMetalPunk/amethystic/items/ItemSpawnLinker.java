package com.IceMetalPunk.amethystic.items;

public class ItemSpawnLinker {
	/*
	 * TODO: Create Spawn Linker with the following properties:
	 * - Used on spawners to "pick it up", copying its NBT data to its own tags
	 * - Full linker can place held spawner down as PlacedSpawner block -- takes
	 * durability
	 * - Register it in the item registry!
	 * - Needs model/texture
	 */
}
